module.exports = async (params) => {
  // const { queue, message } = params;
  //
  // 1. !playlist <playlist name> <command> <url>
  //
  // <play>
  // 1. lookup playlist by playlist name
  // 2. add each song to queue
  //
  // <add>
  // 1. lookup playlist by playlist name
  // 2. add song or reject if already there
  //
  // <remove>
  // 1. lookup playlist by playlist name
  // 2. remove song or reject if missing
};
